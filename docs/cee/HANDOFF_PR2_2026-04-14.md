# Handoff PR2 — CEE Artisan Activation

**Date** : 2026-04-14
**Session précédente** : PR1 + PR2.1 complétés
**Session suivante** : PR2.2 (migration 434 + 7 routes) → PR2.3 (UI) → PR2.4 (tests E2E)

---

## État au moment du handoff

### ✅ Complété
| Item | Fichiers |
|---|---|
| 9 migrations DDL PR1 | `supabase/migrations/424-432_*.sql` |
| Patch sécurité PR1 | `supabase/migrations/433_cee_security_patches.sql` |
| Audit sécurité PR1 | `docs/cee/SECURITY_AUDIT_424_432.md` |
| Smoke tests PR1 | `docs/cee/smoke_tests_424_432.sql` (36 tests) |
| Recon PR2 | `docs/cee/RECON_PR2_ACTIVATION.md` |
| Threat model PR2 | `docs/cee/THREAT_MODEL_PR2.md` (13 MUST) |
| URL safety rules | `docs/cee/PR2_URL_SAFETY.md` |
| Lib helpers PR2.1 | `src/lib/cee/{iban-crypto,yousign,leads-service,emails}.ts` + `.test.ts` (40/40 tests) |
| Types + env | `src/types/admin.ts`, `.env.example` |

### 🚨 À faire IMMÉDIATEMENT (blocker PR2.2)

**Migration 434 — RPC IBAN encrypt/decrypt (SECURITY DEFINER)**

`src/lib/cee/iban-crypto.ts::encryptIban` appelle `supabase.rpc('cee_encrypt_iban')` — la fonction n'existe pas. Créer avant le 1er commit PR2.2 :

```sql
-- supabase/migrations/434_cee_iban_rpc.sql
BEGIN;

CREATE OR REPLACE FUNCTION public.cee_encrypt_iban(p_iban text, p_key text)
RETURNS bytea
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public, pg_temp
AS $$
BEGIN
  IF p_iban IS NULL OR length(p_iban) < 14 THEN
    RAISE EXCEPTION 'Invalid IBAN' USING ERRCODE = 'invalid_parameter_value';
  END IF;
  IF p_key IS NULL OR length(p_key) < 16 THEN
    RAISE EXCEPTION 'Invalid key' USING ERRCODE = 'invalid_parameter_value';
  END IF;
  RETURN pgcrypto.pgp_sym_encrypt(p_iban, p_key);
END;
$$;
REVOKE ALL ON FUNCTION public.cee_encrypt_iban(text, text) FROM PUBLIC;
GRANT  EXECUTE ON FUNCTION public.cee_encrypt_iban(text, text) TO service_role;

CREATE OR REPLACE FUNCTION public.cee_decrypt_iban(p_encrypted bytea, p_key text)
RETURNS text
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public, pg_temp
AS $$
BEGIN
  -- Enforce admin via JWT
  IF (auth.jwt() ->> 'role') <> 'admin' THEN
    RAISE EXCEPTION 'admin only' USING ERRCODE = 'insufficient_privilege';
  END IF;
  RETURN pgcrypto.pgp_sym_decrypt(p_encrypted, p_key);
END;
$$;
REVOKE ALL ON FUNCTION public.cee_decrypt_iban(bytea, text) FROM PUBLIC;
GRANT  EXECUTE ON FUNCTION public.cee_decrypt_iban(bytea, text) TO service_role, authenticated;

-- Alternative ATOMIQUE : store IBAN sans exposer la clé côté Node longtemps
CREATE OR REPLACE FUNCTION public.cee_store_partner_iban(
  p_partner_id uuid, p_iban text, p_bic text, p_titulaire text, p_key text
) RETURNS void
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public, pg_temp
AS $$
DECLARE
  v_last4 char(4);
BEGIN
  -- artisan doit être propriétaire
  IF NOT EXISTS (
    SELECT 1 FROM public.cee_artisan_partners
    WHERE id = p_partner_id AND user_id = auth.uid()
  ) THEN
    RAISE EXCEPTION 'not owner' USING ERRCODE = 'insufficient_privilege';
  END IF;
  v_last4 := substring(regexp_replace(p_iban, '\s', '', 'g') from '.{4}$');
  UPDATE public.cee_artisan_partners
  SET iban_encrypted = pgcrypto.pgp_sym_encrypt(p_iban, p_key),
      iban_last4     = v_last4,
      bic            = p_bic,
      titulaire_compte = p_titulaire,
      updated_at     = now()
  WHERE id = p_partner_id;
END;
$$;
REVOKE ALL ON FUNCTION public.cee_store_partner_iban(uuid, text, text, text, text) FROM PUBLIC;
GRANT  EXECUTE ON FUNCTION public.cee_store_partner_iban(uuid, text, text, text, text) TO authenticated;

COMMIT;
```

**Recommandation** : utiliser `cee_store_partner_iban` (atomique) dans la route `/api/cee/partners/onboarding/iban` — c'est plus sûr que récupérer bytea côté Node puis UPDATE.

---

## Brief PR2.2 — Routes API (prêt à coller dans ralph-coder)

```
PROJET : C:\Users\USER\Downloads\servicesartisans

CONTRAINTES (lire avant de coder) :
- docs/cee/THREAT_MODEL_PR2.md — 13 MUST obligatoires dans le 1er commit
- docs/cee/PR2_URL_SAFETY.md — ne casser AUCUNE URL, routes additives only
- docs/cee/HANDOFF_PR2_2026-04-14.md — contexte complet

ÉTAPE 0 : Créer supabase/migrations/434_cee_iban_rpc.sql (voir HANDOFF §🚨)

ÉTAPE 1 : 7 routes + middleware rate-limit
- src/lib/cee/rate-limit.ts (LRU in-memory 3 req/min par user_id, fallback OK)
- src/lib/cee/csrf.ts (validateOrigin pattern admin-auth)
- src/lib/cee/webhook-idempotency.ts (table cee_webhook_events, INSERT ON CONFLICT DO NOTHING par (source, event_id))
- Routes dans src/app/api/cee/partners/*/route.ts + src/app/api/webhooks/yousign/route.ts

MUST intégrés au 1er commit :
1. validateOrigin() sur toutes les POST
2. rate limit 3/min sur onboarding/iban et training/quiz
3. webhook Yousign: verifyYousignWebhook + timestamp drift + idempotency
4. quiz scoring server-side (body client ignoré, calc via constante QUIZ_QUESTIONS)
5. status transition via updateCeePartnerStatus (throws sur illégale)
6. fail-close si CEE_IBAN_KEY/YOUSIGN_WEBHOOK_SECRET absent en prod

Tests Vitest par route, coverage >=85%.
Build OK + tsc --noEmit 0 erreur + 36+40+nouveaux tests passent.
Aucun commit git.
```

## Brief PR2.3 — UI Wizard (ralph-frontend, après PR2.2)

```
src/app/(private)/espace-artisan/cee/onboarding/
├── page.tsx                  # Server: auth guard + fetch partner record via GET me
├── OnboardingWizard.tsx      # Client: state machine 5 steps (welcome/iban/convention/training/activate)
├── steps/
│   ├── Welcome.tsx           # Rôle SA Energy
│   ├── Iban.tsx              # Form IBAN+BIC+titulaire, validation live, POST onboarding/iban
│   ├── Convention.tsx        # Trigger POST convention → iframe Yousign signerUrl
│   ├── Training.tsx          # 4 vidéos Wistia + Quiz 10Q, POST training/quiz
│   └── Activate.tsx          # Récap + CTA POST activate + redirect /espace-artisan/cee
└── ConventionPDF.tsx         # @react-pdf/renderer, 6 mentions arrêté 2/11/2023

WCAG 2.1 AA: labels explicites, aria-live sur erreurs, focus trap wizard, contrast ≥4.5.
8 states par composant. Light-only (pas de dark:*).
```

## Brief PR2.4 — Tests E2E (ralph-tester, parallèle PR2.3)

```
Tests 1-13 du THREAT_MODEL_PR2.md §5.
__tests__/api/cee/partners/*.test.ts (un fichier par route).
Mock Yousign HTTP via vi.mock fetch.
```

---

## À faire par l'humain

### Dès que possible
1. **Supabase SQL editor** : apply 424→433 (split BEGIN/COMMIT, cf. supabase_sql_editor_quirks)
2. **Run smoke_tests_424_432.sql** par blocs A/B/C/D/E
3. **Vercel env vars** :
   - `CEE_IBAN_KEY` = 32 bytes aléatoires base64 (`openssl rand -base64 32`)
   - `YOUSIGN_API_KEY` = fourni par Yousign dashboard
   - `YOUSIGN_WEBHOOK_SECRET` = fourni par Yousign webhook settings

### Avant PR2.2 merge
4. Migration 434 appliquée en prod
5. Tests sécurité 1-13 tous verts
6. Build Vercel preview OK, sitemap.xml hash inchangé

### Ordres d'apply migrations
425 → 424 → 426 → 427 → 428 → 429 → 430 → 431 → 432 → 433 → 434

---

## Commande de reprise session suivante

```
Lis docs/cee/HANDOFF_PR2_2026-04-14.md. Crée migration 434 d'abord,
puis lance ralph-coder avec le brief PR2.2. run_in_background=true.
```
